# Audit Analysis Tools

This directory contains standalone audit analysis scripts, tools, and historical sprint reports (`SPRINT1_AUDIT.md`, etc.). 
It is entirely separate from `lib/audit/`, which contains the actual runtime audit engine for the Proposal Engine OS application.

If you are looking for the runtime application code that powers the audit feature, please see `lib/audit/`.
