/**
 * Canonical audit module list. Single and batch audits MUST use this exact set.
 */
export {
  CANONICAL_AUDIT_MODULES as CANONICAL_MODULES,
  type CanonicalAuditModuleId as CanonicalModuleId,
} from '@shared/audit';
