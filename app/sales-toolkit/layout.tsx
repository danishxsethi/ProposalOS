import './sales-toolkit.css';

export const metadata = {
  robots: 'noindex, nofollow',
};

export default function SalesToolkitLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
